{
  "schemaVersion": "1.0",
  "fixtures": [
    {
      "fixtureId": "cube_ok",
      "taxonomy": ["topology", "holes"],
      "expectedManifest": "topology/holes/expected.json",
      "notes": "Baseline hole fixture used for warnings/repair expectations."
    },
    {
      "fixtureId": "nonmanifold_edge",
      "taxonomy": ["topology", "non-manifold"],
      "expectedManifest": "topology/non-manifold/expected.json"
    },
    {
      "fixtureId": "tiny_shells",
      "taxonomy": ["stress", "dense-noisy-scans"],
      "expectedManifest": "stress/dense-noisy-scans/expected.json"
    }
  ]
}
